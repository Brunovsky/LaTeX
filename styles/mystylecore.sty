% Set pdf's output font encoding to T1 (Standard)
\usepackage[T1]{fontenc}

% Set file input encoding to utf8 (Standard)
\usepackage[utf8]{inputenc}

% Include geometry package (Standard)
% With verbose, geometry dumps it's core variables into the .log file
\usepackage[verbose]{geometry}

% Use latin modern font
\usepackage{lmodern}

% Include textcomp for extra text symbols
\usepackage{textcomp}

% Improve font visual quality and kerning capabilities (Standard)
\usepackage{microtype}

% Include cal for infix notation arithmetic (Standard)
\usepackage{calc}

% Include \syntaxonly to suppress output and check syntax
\usepackage{syntonly}

% Include subfiles to ease project layout into multiple files
\usepackage{subfiles}